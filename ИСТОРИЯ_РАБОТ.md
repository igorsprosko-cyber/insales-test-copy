# ИСТОРИЯ РАБОТ — VELES LEGS (veles-legs.ru)

Единый рабочий журнал проекта. Перед каждой новой работой читать актуальное состояние и проверять HEAD напрямую в GitHub.

**Актуализировано:** 03.09.2026  
**Рабочая ветка:** `v2.3-page-standardization-final2`  
**Production:** `main` — не изменять без прямого разрешения  
**Reference:** `v2.3-page-standardization` — не изменять

---

# 1. ПРОЕКТ И ВЕТКИ

Репозитории:

- `my-first-project` — источник правил, архитектуры и контекста;
- `insales-test-copy` — рабочий код сайта;
- `foto-redaktor` — отдельный проект.

Ветки:

- `main` — production;
- `v2.3-page-standardization` — эталон;
- `v2.3-page-standardization-final2` — текущая рабочая ветка.

На финальной сверке:

- working HEAD до документационной записи: `8f8c2b72891b1b3059450fbc79a691e67c072822`;
- `main`: `fcbf3daa9d25b9a469757e21501912e903667aed`;
- reference point: `046f15e4ac5dae7f6b665314b824501cc8816259`.

Аудиторская серия после `10a171054eb523b0774c81157d70e3f92ef08710` меняла только документы.

---

# 2. ЖЁСТКИЕ ПРАВИЛА

- Цикл работы: **ПРОЧИТАНО → ПРОАНАЛИЗИРОВАНО → ИЗМЕНЕНО → ПЕРЕЧИТАНО → ПРОВЕРЕНО → DIFF**.
- Не выдумывать результаты. Нет доказательства — `NOT VERIFIED`.
- После записи проверять SHA и фактический diff.
- Не делать массовый refactor без отдельного задания.
- История проекта ведётся только здесь; новые history-файлы не создавать.
- Не использовать JS-инъекцию как замену точечной CSS/Liquid-правке.
- `main` и reference branch не трогать.
- Калькулятор — защищённая зона.
- Не менять `calcWood`, `handleWood*`, `openManagerForm`, `fillComment`, `wood-*`, `panel-wood`, `panel-metal`, цены, НДС, скидки, вес, объём и бизнес-логику.
- Metal Routing считать известной незавершённой функцией, если нет отдельного задания.

---

# 3. ХРОНОЛОГИЯ ОСНОВНЫХ РАБОТ

| Commit | Зафиксированная работа |
|---|---|
| `b3441fb` | Удалены 2 мёртвых design-файла. |
| `eff1530` | Block 1: устранён конфликт Hero object-fit, исправлена типографика, удалён старый Hero JS-костыль. |
| `e417b76` | Block 2: классификация `!important`, исправлены 2 реальные CSS-конфликта, `styles.liquid` возвращён к эталону. |
| `c874a81` | `nth-of-type` заменены явными классами секций. |
| `5af388b` | 2 статичных inline-style перенесены в CSS; calculator JS не тронут. |
| `2fb309c` | `noindex, follow` для search/favorite/compare. |
| `0890ca4` | Title/description для favorite/compare. |
| `1b17cff` | 60 spacing values приведены к 4px-сетке. |
| `1f1996e` | Динамический `og:type`. |
| `87b748d` | Исправлена логика AggregateOffer через документированный sort; availability через `product.available`. |
| `3808ee0` | Удалены временный baseline и старый draft homepage. |
| `b196426` | Создан единый `ИСТОРИЯ_РАБОТ.md`. |
| `9e80784` | Полный QA Blocks 1–5. |
| `b002993` | Accessibility: aria-label для contact fields и focus-visible для select groups. |
| `aae6407` | Независимая A11Y-попытка через `head.liquid`, позднее признана дублирующей. |
| `276a133` | A11Y-консолидация; полезные focus rules оставлены в `index.liquid`. |
| `133e724` | Исправлены визуальные баги Hero/VELES LIGHT/«Собственное производство»; подтверждено пользователем. |
| `bd049a2` / `5383cb3` / `e608790` | Кнопки удаления товара из сравнения. |
| `04bbb20` | По разрешению пользователя стандартизированы шрифты/акцентный цвет; `#76BC21` → `#5E8C31`. |
| `6e9793d` | aria-label для 6 условных полей калькулятора «Другое». |
| `e8689fdc` | Уплотнена compare page через системные InSales layout variables; desktop/mobile приняты пользователем. |
| `6a612584` | Визуальная проверка compare объединена в единый журнал. |
| `f607622` | Добавлены реальные фото в 4 категории. |
| `ef63303` → `e98de4c` | Ошибочная серия JS-overrides category cards; затем признана и устранена. |
| `6905785` | Удалён JS-hack category cards; возвращена чистая CSS-логика; исправлены имена 4 image files. |
| `10a171...` | Последнее изменение исходного кода перед аудиторской серией. |
| `f40fa4a...` | Первоначальный `СТРЕС АНАЛИЗ.md`. |
| `8f8c2b...` | Документальная фиксация mobile PageSpeed. |
| `a159b196...` | Финальная reconciliation audit с последним desktop report. |

---

# 4. ВАЖНЫЕ УРОКИ И ИНЦИДЕНТЫ

## JS вместо CSS

Category-card история доказала: CSS/Liquid проблему нельзя лечить JS-инъекциями с `!important`. Такой подход создавал белую полосу, отдельный текстовый блок и разрушал композицию.

## Qwen Coder

Ранее независимая проверка выявила несанкционированный refactor `theme.scss` и фабрикацию VERIFIED-отчёта. Все результаты других ИИ проверять независимо по Git/diff/runtime.

## Compare

Удаление товара должно находиться внутри фактической product-card. Пользователь визуально подтвердил desktop/mobile после compacting.

---

# 5. ПОДТВЕРЖДЁННЫЕ РЕЗУЛЬТАТЫ

- Основные visual fixes `133e724` подтверждены пользователем.
- Compare desktop/mobile после `e8689fdc` подтверждены пользователем.
- 6 conditional calculator fields получили accessible labels.
- Calculator business logic не менялась в аудиторской серии.
- Category JS-hack был удалён и не должен возвращаться.
- `main` и reference branch не менялись в рамках аудиторской серии.

---

# 6. PAGE SPEED — ВСЕ СОХРАНЁННЫЕ СРЕЗЫ

## 6.1 Старый desktop baseline

| Метрика | Значение |
|---|---:|
| Performance | 49 |
| FCP | 0,8 с |
| LCP | 4,7 с |
| TBT | 540 мс |
| CLS | 0,016 |
| Speed Index | 2,5 с |
| Accessibility | 96 |
| Best Practices | 96 |
| SEO | 100 |

Главные находки: тяжёлые изображения, Hero LCP, third-party JS, fonts, >4 preconnect, forced reflow, высокий image payload.

## 6.2 Mobile baseline

Источник: пользовательский mobile PageSpeed, Moto G Power, Lighthouse 13.4.1, initial load, slow 4G.

| Метрика | Mobile |
|---|---:|
| Performance | 39 |
| FCP | 3,6 с |
| LCP | 27,5 с |
| TBT | 940 мс |
| CLS | 0,018 |
| Speed Index | 8,8 с |
| Accessibility | 96 |
| Best Practices | 96 |
| SEO | 100 |
| Agentic browsing | 3/3 |
| TTFB | 50 мс |

Mobile diagnostics:

- main-thread около 4,0 с;
- Script Evaluation 1 602 мс;
- Style & Layout 1 015 мс;
- Script Parsing & Compilation 902 мс;
- 14 long tasks;
- image potential savings около 2 906 KiB;
- total transferred payload около 7 377 KiB;
- Hero `opora-a178-chrome.jpg` около 2 200,8 KiB;
- `stol-vintage.jpg` около 2 924,8 KiB;
- Metrica/InSales/Yandex/GTM дают заметный CPU вклад.

## 6.3 Последний desktop baseline — 03.09.2026 07:41 GMT+3

Источник: новый пользовательский PageSpeed report. Это **desktop**, не mobile.

Условия: computer emulation, Lighthouse 13.4.1, initial load, custom throttling, HeadlessChromium 151.0.7922.71.

| Метрика | Новый desktop |
|---|---:|
| Performance | **65** |
| FCP | **0,9 с** |
| LCP | **5,1 с** |
| TBT | **190 мс** |
| CLS | **0** |
| Speed Index | **2,6 с** |
| Accessibility | **96** |
| Best Practices | **96** |
| SEO | **100** |
| Agentic browsing | **3/3** |

### Сравнение со старым desktop

- Performance: 49 → 65;
- TBT: 540 → 190 мс;
- CLS: 0,016 → 0;
- FCP: 0,8 → 0,9 с;
- Speed Index: 2,5 → 2,6 с;
- LCP: 4,7 → 5,1 с.

Это не считать доказанным эффектом конкретного commit: между измерениями меняются runtime/external-resource conditions, а после `10a171...` код в audit series не менялся.

---

# 7. IMAGE PERFORMANCE — ФИНАЛЬНАЯ ФИКСАЦИЯ

Новый desktop report показывает ожидаемую экономию изображений около **5 882 KiB**.

Главные ресурсы:

- `stol-vintage.jpg`: 2 924,1 KiB; потенциальная экономия около 2 900,8 KiB; 916×1024 при container около 378×378;
- `opora-a178-chrome.jpg`: 2 200,8 KiB; потенциальная экономия около 2 156,5 KiB; 848×1111 при container около 428×637;
- `foto_furnitura.jpg`: 170,6 KiB;
- `foto_derevo.jpg`: 135,0 KiB;
- `foto_metal.jpg`: 113,1 KiB;
- `foto_nazakaz.jpg`: 110,4 KiB;
- `veles-light-photo.jpg`: 160,0 KiB;
- `veles-light-logo.png`: 117,4 KiB;
- `veles-logo-realistic-clover.png`: 81,0 KiB.

Два крупнейших изображения дают примерно **5,13 MiB**.

Новый total payload: около **7 275 KiB**.

Решение:

1. Hero — responsive size + modern format + early discovery + high priority + без lazy;
2. `stol-vintage` — responsive + modern format + lazy, если ниже fold;
3. category/light/logo — вторым image pass;
4. после каждого pass повторять mobile и desktop.

---

# 8. JAVASCRIPT / MAIN THREAD

Новый desktop:

- JS execution около 1,0 с;
- Script Evaluation 768 мс;
- Parsing & Compilation 411 мс;
- Style & Layout 228 мс;
- main-thread work около 1,6 с;
- 9 long tasks.

Mobile:

- main-thread 4,0 с;
- Script Evaluation 1 602 мс;
- Style & Layout 1 015 мс;
- Parsing & Compilation 902 мс;
- 14 long tasks.

Следствие: JS/layout — P1, но не должен отвлекать от двух гигантских изображений P0.

---

# 9. THIRD-PARTY / SYSTEM

Последний desktop report:

| Источник | Передано | CPU/main-thread |
|---|---:|---:|
| Yandex Metrica | 184 KiB | 312 мс |
| InSales | 6 255 KiB | 219 мс |
| Google Tag Manager | 123 KiB | 154 мс |
| yandex.ru | 157 KiB | 138 мс |
| Yandex CDN partner | 293 KiB | 118 мс |
| SmartCaptcha | 81 KiB | 63 мс |

Mobile был существенно тяжелее: Metrica 727 мс, InSales 596 мс, Yandex 482 мс, GTM 427 мс.

Не удалять внешние сервисы вслепую. Сначала получить inventory tags/widgets и trace.

---

# 10. NETWORK / PRECONNECT / FONTS

Последний desktop report:

- critical path max delay **1 556 мс**;
- `/front_api/cart.json` до 1 556 мс;
- `common.v2.27.9.js` около 905 мс в цепочке;
- core-css/jQuery и вспомогательные InSales assets участвуют в critical chain;
- Google Fonts CSS + 4 WOFF2 участвуют в цепочке;
- preconnect больше четырёх;
- Lighthouse считает `yastatic.net`, `avatars.mds.yandex.net`, `mc.yandex.ru`, `ads.adfox.ru` неиспользуемыми preconnect.

Решение: waterfall → определить реально критичные источники → оставить только нужные preconnect → затем fonts.

---

# 11. ACCESSIBILITY

Последний desktop: 96.

Mobile ранее выявил contrast warning для `.vl-v2-why-number`.

Последний desktop также показывает два одинаковых `textarea#order_comment` в quick checkout modal. Это требует runtime investigation duplicate ID.

Одновременно Lighthouse подтверждает labels, alt, доступные button names, touch targets, heading order, main landmark и lang.

Статус: **GOOD / CONTRAST ISSUE / DUPLICATE-ID INVESTIGATION**.

Не менять цвета до локализации ratio и конкретного selector.

---

# 12. SEO / STRUCTURED DATA

Lighthouse SEO = 100 в обоих последних срезах.

Зафиксирована база:

- canonical;
- title/description;
- Organization JSON-LD;
- BreadcrumbList;
- OG;
- noindex функциональных страниц;
- Article schema;
- 404 template.

Остаются отдельные задачи:

- Product schema использует AggregateOffer и требует сверки с реальными Offer/variants;
- проверить соответствие merchant/product structured data фактическим ценам и товарам;
- проверить Search Console и Yandex Webmaster, поскольку Lighthouse не показывает реальные indexation/visibility data.

---

# 13. CONTENT / TRUST / LEGAL

Зафиксированные несогласованности:

1. minimum order: schema «От 100» против публичных Wood pages «От 40»;
2. claims по нагрузке металла: 100–200 кг и отдельные 500 кг+ statements;
3. `sensatsiya` содержит конфликтующие claims;
4. A-04 требует сверки title/description/material/country;
5. public offer содержит старый домен `veleslight.ru` при текущем `veles-legs.ru`;
6. offer частично B2C при сильном B2B-позиционировании;
7. формулировки о персональных данных требуют юридической проверки.

Это отдельный content/trust/legal pass, не смешивать с image optimization.

---

# 14. SECURITY / BEST PRACTICES

Последний report Best Practices = 96.

Обнаружено:

- GTM `gtag` resource 404;
- Metrica WebSocket не установился в тесте;
- нет enforced CSP;
- нет HSTS preload directive;
- нет COOP;
- нет Trusted Types CSP directive.

Это findings для hardening, не автоматическое доказательство уязвимости. Проверять ограничения InSales перед изменением headers.

---

# 15. DOM / AI READINESS

Последний desktop:

- около 798 DOM elements;
- depth 16;
- body до 41 child.

Не считать DOM экстремальным и не делать массовый cleanup без trace.

Agentic browsing = 3/3:

- accessibility tree сформирован;
- `llms.txt` соответствует рекомендациям;
- AI info page присутствует;
- WebMCP checks в дополнительных/manual категориях.

---

# 16. VERIFIED / PARTIALLY VERIFIED / NOT VERIFIED

## VERIFIED

- branch refs проверены;
- `main` не изменялся;
- reference branch не изменялся;
- audit series не меняла source code;
- пользовательские PageSpeed numbers зафиксированы;
- mobile/desktop разделены;
- image payload подтверждён отчётами;
- compare visual QA подтверждён пользователем;
- calculator accessibility labels зафиксированы;
- Agentic 3/3;
- SEO 100.

## PARTIALLY VERIFIED

- canonical/schema architecture;
- image loading behavior;
- LCP discovery/priority;
- theme.js contribution;
- InSales widget contribution;
- third-party configuration;
- category runtime;
- console error significance.

## NOT VERIFIED

- real-user CWV / CrUX;
- GSC data;
- Yandex Webmaster data;
- exact GTM inventory;
- exact Metrica configuration/goals;
- physical mobile waterfall;
- exact root cause of 27,5 s mobile LCP;
- production-vs-branch runtime equivalence;
- exact source of every forced-layout event.

---

# 17. ФИНАЛЬНАЯ ПРИОРИТЕТНАЯ МАТРИЦА

### P0

1. Mobile LCP 27,5 с.
2. Hero 2,2 MiB.
3. `stol-vintage` 2,9 MiB.
4. LCP discovery/priority.

### P1

5. Image payload total.
6. Mobile main-thread 4,0 с.
7. Long tasks.
8. Metrica/GTM/Yandex/InSales.
9. Style & Layout.
10. Critical request chain.
11. Preconnect.
12. Contrast.
13. GTM console 404.
14. Product schema.
15. Content truth inconsistencies.
16. Legal/trust inconsistencies.

### P2

17. Category image sizing.
18. Logos.
19. Fonts after image pass.
20. DOM cleanup only if trace supports.
21. MutationObserver profiling.
22. CSS architecture cleanup as separate refactor.

---

# 18. ПЛАН СЛЕДУЮЩИХ РАБОТ

## Phase 0 — freeze baseline

Сохранить mobile + старый desktop + новый desktop reports и SHA.

## Phase 1 — forensic

Chrome Performance trace + Network waterfall.

## Phase 2 — image-only

Hero + `stol-vintage` + responsive formats/sizes/loading.

## Phase 3 — retest

Mobile + desktop PageSpeed + visual QA + diff.

## Phase 4 — third-party/JS

GTM/Metrica/Yandex/InSales inventory, defer/conditional loading только там, где доказано безопасно.

## Phase 5 — layout

Forced layout, MutationObserver, style recalculation. CLS сохранить.

## Phase 6 — accessibility

Contrast + duplicate ID.

## Phase 7 — SEO/data/legal

Schema, product truth, minimum order, load claims, offer/privacy/domain.

## Phase 8 — architecture

Только после измерений; небольшими изолированными коммитами.

---

# 19. КОНТРОЛЬНЫЕ TARGETS

Targets, не обещания:

### Mobile

- LCP 27,5 с → целиться ≤2,5 с;
- FCP 3,6 с → целиться <1,8 с;
- TBT 940 мс → существенно ниже;
- CLS 0,018 → сохранить ≤0,1;
- Speed Index 8,8 с → существенно ниже.

### Desktop

- LCP 5,1 с → целиться ≤2,5 с;
- TBT 190 мс → сохранить низким;
- CLS 0 → сохранить 0/≤0,1.

Цель — реальное улучшение CWV, а не только Lighthouse score.

---

# 20. ФИНАЛЬНАЯ АНТИСТРЕСС-ЗАПИСЬ

На 03.09.2026 у проекта есть полноценная доказательная база для следующего этапа.

**Не переписывать тему.**

**Не трогать калькулятор.**

**Не лечить CSS JS-костылями.**

**Не удалять системные/third-party ресурсы вслепую.**

Работать по цепочке:

**LCP image → image payload → discovery/priority → mobile JS → third-party → layout → accessibility → schema/content/legal → architecture.**

Вся история performance теперь содержит три раздельных среза: старый desktop, mobile и последний desktop. Это позволяет не путать улучшение TBT/CLS с ухудшением LCP и не приписывать разницу конкретному коду без доказательства.

**В рамках этой финальной аудиторской работы исходный код сайта не изменялся. Обновлены только `СТРЕС АНАЛИЗ.md` и этот единый `ИСТОРИЯ_РАБОТ.md`.**
