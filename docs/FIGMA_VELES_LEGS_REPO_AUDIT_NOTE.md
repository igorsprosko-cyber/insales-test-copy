# VELES LEGS — Repository Audit Note

The complete byte-level audit of every file in every repository is not fully verified because this execution environment could not perform a local Git clone and the GitHub connector cannot expose binary archive/image internals. Direct GitHub source/config/document reads were used instead.

The factual Figma↔code map is in `docs/FIGMA_VELES_LEGS_CODE_MAP.md`. Repository-level scope and verification limits are recorded here and in `docs/FIGMA_VELES_LEGS_AUDIT_STATUS.md`.
